A command‑line utility that reads newline‑delimited JSON logs from standard input, normalises all timestamp fields to UTC (converting timezone offsets and the `Z` suffix), discards duplicate entries that share the same normalised timestamp, and writes the cleaned JSON lines to standard output. Malformed JSON, missing or unparseable timestamps, and duplicate entries are silently skipped while a warning is printed to standard error.